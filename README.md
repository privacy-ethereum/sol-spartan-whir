# sol-spartan-whir

Solidity verifier for the Spartan-WHIR SNARK over the KoalaBear field (p = 2^31 - 2^24 + 1).

This repository contains the Foundry verifier project. Test fixtures in `testdata/` are checked in and generated by the companion Rust exporter, [spartan-whir-export](https://github.com/alxkzmn/spartan-whir-export), from the Rust [Spartan-WHIR implementation](https://github.com/alxkzmn/spartan-whir).

## Build and test

```sh
forge build
forge test
```

Test fixtures in `testdata/` are checked in, so the test suite runs without any external setup. To regenerate them, point `SPARTAN_WHIR_EXPORT_DIR` at a checkout of [spartan-whir-export](https://github.com/alxkzmn/spartan-whir-export) and invoke the exporter:

```sh
cargo run --release --manifest-path "$SPARTAN_WHIR_EXPORT_DIR/Cargo.toml" --bin export-fixtures -- testdata
```

The exporter depends on the Rust [Spartan-WHIR implementation](https://github.com/alxkzmn/spartan-whir), which is the source of truth for the protocol; the Solidity verifier in this repository must stay byte-compatible with it.

See [AGENTS.md](./AGENTS.md) for the full command catalog: schedule-specific fixture exporters, gas profiling and flamegraph workflows, transaction-gas benchmarking, the precompile-runner Foundry fork ([alxkzmn/foundry](https://github.com/alxkzmn/foundry/tree/codex/ext8-precompile-runner)), and the upstream verifier source anchors.

## Current verifier target

The current build target is the quintic schedule `k22_jb100_ext5_lir4_ff4_rsv3_pow28` (`QuinticTrinomialExtensionField<KoalaBear>` with `X^5 + X^2 - 1`), targeting `num_variables = 22` at JohnsonBound 100-bit security:

| Item                               |            Value |
| ---------------------------------- | ---------------: |
| folding schedule                   |    `Constant(4)` |
| requested PoW bits                 |             `28` |
| starting log inverse rate          |              `4` |
| RS domain initial reduction factor |              `3` |
| achieved security bits             |       `100.0145` |
| achieved Merkle security bits      |            `160` |
| measured prover time               | `274.163664792s` |
| native blob Foundry gas            |      `5,526,933` |
| native blob transaction gas        |      `5,646,080` |
| native blob execution gas          |      `4,768,744` |
| native blob calldata bytes         |         `54,436` |

The transaction gas rows are measured with Anvil for this target. The execution
gas figure is the transaction gas with the `21,000` base cost and the calldata
cost subtracted, so it reflects only the code that runs inside the verifier
contract.

The previous quintic build target, `k22_jb100_ext5_lir4_ff4_rsv4`, is still
checked in for comparison. It uses `pow_bits = 27`,
`rs_domain_initial_reduction_factor = 4`, measured `133.516761291s` prover
time, measures `6,447,048` native blob transaction gas with the same base row
and pairwise select evaluation kernels, and measures `6,357,321` in the
Foundry native blob gas test.

### Native blob deployability

None of the checked-in quintic native blob verifier variants is deployable as a
single contract under EIP-170 at the current default build settings:

| Native blob verifier variant       | Runtime bytecode | EIP-170 status |
| ---------------------------------- | ---------------: | -------------- |
| `rsv3_pow28` software              |       `33,119` B | `8,543` B over |
| `rsv4` software                    |       `32,870` B | `8,294` B over |
| `rsv3_pow28` local precompile path |       `29,551` B | `4,975` B over |

Optimizer-run tuning alone does not make these native blob contracts fit. The
best low-run checks found `29,529` bytes for `rsv3_pow28` software,
`29,818` bytes for `rsv4` software, and `26,156` bytes for the local
precompile-backed `rsv3_pow28` verifier.

Generated fixture prefix:

```text
quintic_whir_k22_jb100_ext5_lir4_ff4_rsv3_pow28
```

Generated fixed config:

```text
src/generated/QuinticWhirFixedConfig_k22_jb100_ext5_lir4_ff4_rsv3_pow28.sol
```

Native transaction benchmark script:

`script/WhirBlobNativeTxBenchmark_k22_jb100_ext5_lir4_ff4_rsv3_pow28.s.sol`

Schedule selection across candidate WHIR parameter sets is driven by `whir_param_sweep.py` (calibrated execution-gas model) and `quintic_schedule_scorer.py` (Pareto plots over verifier work versus measured prover time). These are estimation tools; the deployed verifier source is the source of truth.

## Gas

### Cross-schedule phase breakdown

Phase-level verifier cost across the checked-in high-security software native blob verifiers, measured with `gasleft()` harnesses. Headline transaction-gas and calldata numbers for these schedules are listed in the schedule-specific subsections; this view is complementary and tracks where execution gas is spent inside the verifier call.

| Family  | Schedule                             |    Setup | Initial sumcheck | R0 parse |   R0 STIR | R0 sumcheck | R1 parse |   R1 STIR | R1 sumcheck | R2 parse |   R2 STIR | R2 sumcheck | Observe final poly | Final STIR | Final sumcheck | Constraint evaluation | Final value check |   Phase sum |
| ------- | ------------------------------------ | -------: | ---------------: | -------: | --------: | ----------: | -------: | --------: | ----------: | -------: | --------: | ----------: | -----------------: | ---------: | -------------: | --------------------: | ----------------: | ----------: |
| quintic | `k22_jb100_ext5_lir4_ff4_rsv3_pow28` | `20,864` |         `27,353` |  `2,799` | `703,346` |    `27,243` |  `2,784` | `925,758` |    `27,301` |  `2,793` | `582,564` |    `27,209` |           `63,759` |  `669,008` |       `37,047` |           `1,512,664` |         `135,805` | `4,768,297` |
| octic   | `k22_jb100_lir6_ff4_rsv1`            | `28,015` |         `44,354` |  `3,741` | `632,941` |    `44,318` |  `3,747` | `882,959` |    `44,312` |  `3,738` | `678,217` |    `44,309` |           `90,344` |  `850,870` |       `67,103` |           `1,948,563` |         `222,114` | `5,589,645` |

The phase rows come from the native compare harness and the quintic gas-profile harness listed in [AGENTS.md](./AGENTS.md).

### Quintic software profile

The current quintic build target was profiled with the native blob verifier flamegraph harness.

The canonical Foundry gas for this path is `5,526,933`. The verifier call itself, excluding fixture-loading harness noise, accounts for `4,768,744` gas.

| Profile bucket                                                             |         Gas | Share of verifier call |
| -------------------------------------------------------------------------- | ----------: | ---------------------: |
| Inlined verifier body, mostly fused STIR row evaluation and blob traversal | `3,290,290` |                `67.8%` |
| Final STIR and final-polynomial checks                                     |   `655,138` |                `13.5%` |
| Explicit ext5 `mul` / `square` / `sub` functions                           |   `283,884` |                 `5.8%` |
| Final value evaluation                                                     |   `134,938` |                 `2.8%` |
| Base-field powers and query-point generation                               |   `141,586` |                 `2.9%` |
| Equality terms and dim-4 equality-weight precomputation                    |   `160,962` |                 `3.3%` |
| Merkle frontier computation                                                |   `104,935` |                 `2.2%` |
| STIR query sampling and transcript sampling                                |   `112,117` |                 `2.3%` |
| Extension inversion or division                                            |         `0` |                 `0.0%` |

The profile does not show the fused row dot-product kernels as separate function calls because `via_ir` inlines them into the verifier body. The dominant arithmetic work is therefore repeated WHIR row and final evaluation, with extension multiplication embedded inside fused multiply-accumulate kernels. It is not extension inversion or division, and it is no longer mostly standalone calls to `KoalaBearExt5.mul`.

### Comparison: `sol-spartan-whir` vs `sol-whir`

| Metric                    |                           `sol-spartan-whir` |                            `sol-whir` |
| ------------------------- | -------------------------------------------: | ------------------------------------: |
| Main verifier call        | `EOA -> WhirBlobVerifierNative4.verify(...)` | `EOA -> WhirContract.callVerify(...)` |
| Field                     |                             KoalaBear + ext4 |                                 BN254 |
| Security target           |                                       80-bit |                                80-bit |
| Total verification tx gas |                                    `975,202` |                           `1,135,052` |
| Intrinsic gas             |                                     `21,000` |                              `21,000` |
| Calldata gas              |                                    `159,640` |                             `414,876` |
| Execution remainder       |                                    `794,562` |                             `699,176` |
| Calldata bytes            |                                     `10,276` |                              `28,740` |
| Zero bytes                |                                        `398` |                               `3,747` |
| Non-zero bytes            |                                      `9,878` |                              `24,993` |

Notes:

- `sol-spartan-whir` tx numbers are measured from `WhirBlobVerifierNative4` in this repository.
- `sol-whir` tx numbers come from the broadcast artifact committed at [privacy-ethereum/sol-whir: broadcast/Verify.s.sol/31337/run-latest.json](https://github.com/privacy-ethereum/sol-whir/blob/main/broadcast/Verify.s.sol/31337/run-latest.json)
- Foundry execution gas for `WhirBlobVerifierNative4`: `899,906`
- `WhirBlobVerifierNative4` runtime size: `21,877` bytes
- success blob size: `10,152` bytes

#### Arithmetic and proof-structure differences

The execution-gas gap comes from both arithmetic cost and proof-structure differences. The table below lists the benchmark parameters that matter most.

| Item                               | sol-spartan-whir | sol-whir |
| ---------------------------------- | ---------------- | -------- |
| **Field / arithmetic**             | KoalaBear + ext4 | BN254    |
| **Configured WHIR rounds**         | 2                | 3        |
| **Per-round query counts**         | 9, 6             | 9, 6, 5  |
| **Final query count**              | 5                | 4        |
| **Total STIR queries incl. final** | 20               | 24       |
| **Commitment OOD samples**         | 2                | 1        |
| **Final sumcheck rounds**          | 4                | 0        |

Implications:

- `sol-whir` has one extra configured round and `4` more total STIR queries.
- `sol-spartan-whir` pays for more OOD sampling and a real final sumcheck.
- The one structural delta we can price directly is the final sumcheck: ~`27.1k` gas in `sol-spartan-whir`, while the `sol-whir` benchmark config has `finalSumcheckRound = 0`.
- Proof structure explains part of the gap; the rest is execution cost inside the verifier.

### Extension degree choice for k=22 / JohnsonBound 100-bit

The quintic extension is the current high-security build target. Quartic (~124 field bits) does not have the soundness budget for `num_variables = 22` at JohnsonBound 100-bit without large grinding caps or query counts that land in a high-gas regime; octic (~248 field bits) has more headroom than needed and costs more per query. Quintic (~155 field bits) sits in the middle: it supports `oodSamples = 1` at this security target with extension arithmetic cost closer to quartic than to octic. In this README, quartic is retained as the 80-bit baseline for the `sol-whir` comparison, not as the high-security deployment target.

### Other Schedules

#### Octic JohnsonBound example

This repository also includes a separate octic standalone-WHIR configuration for the `2^22` JohnsonBound target:

- schedule: `22` variables, JohnsonBound soundness target, folding factor `4`, starting log inverse rate `6`, RS domain initial reduction factor `1`
- contract file: `src/whir/k22_jb100_lir6_ff4_rsv1/WhirBlobVerifierNative8_k22_jb100_lir6_ff4_rsv1.sol`
- fixture family: `export-fixtures-octic-k22-jb100-lir6-ff4-rsv1`

Current Foundry metrics for `WhirBlobVerifierNative8_k22_jb100_lir6_ff4_rsv1`:

| Metric                                                                       |          Value |
| ---------------------------------------------------------------------------- | -------------: |
| `WhirBlobVerifierNative8K22Jb100Test.testGasWhirVerifyBlobNativeFixed()`     |    `6,085,570` |
| `WhirBlobVerifierNative8K22Jb100Test.testVerifyOcticWhirSuccessBlobNative()` |    `6,085,658` |
| `WhirVerifier8K22Jb100Test.testGasWhirVerifyFixed()`                         |    `8,843,827` |
| deployed bytecode                                                            | `37,794` bytes |

Transaction/calldata metrics for the same proof blob:

| Metric                                                                |          Value |
| --------------------------------------------------------------------- | -------------: |
| total tx gas from `WhirBlobNativeTxBenchmark_k22_jb100_lir6_ff4_rsv1` |    `6,367,262` |
| execution remainder                                                   |    `5,592,462` |
| `verify(bytes32,bytes)` calldata bytes                                |       `47,780` |
| `verify(bytes32,bytes)` calldata gas                                  |      `753,800` |
| success blob size                                                     | `47,666` bytes |

The calldata number above is the exact ABI-encoded call payload for `verify(bytes32,bytes)`: function selector, expected commitment, dynamic-bytes head, blob payload, and trailing padding.

#### Applied octic optimizations

- The octic contract is `WhirBlobVerifierNative8_k22_jb100_lir6_ff4_rsv1`. Its external entrypoint is `verify(bytes32 expectedCommitment, bytes blob)`, and it reads the proof blob directly from calldata.
- The STIR row kernel is specialized for `rowLen=16` and uses the combined blob-side hash-and-evaluate path with packed 20-byte frontier reduction.
- Ext8 folding uses direct arithmetic kernels, including the Karatsuba-based `_foldOnceWithCoeffs(...)` implementation used in the contract.
- `KoalaBearExt8.square` uses a fused tower-style implementation behind the existing packed ext8 API.
- Extension-row STIR dot products use a local-values tower helper over the 16 packed row values and packed equality weights.
- Round-constraint accumulation uses a tower helper for the fixed `18/14/10` select products instead of generic ext8 multiplication.
- Fixed equality-term accumulation fuses `acc * eq(p, q)` into one tower helper.
- Final STIR uses two fixed five-query checks for the `64`-coefficient final polynomial.
- Final-value evaluation uses a fixed scratch-memory fold tree instead of a dynamic `evals` array.
- [whir_param_sweep.py](./whir_param_sweep.py) models this octic configuration, but its octic gas calibration row still uses `7,383,992`. The measured native blob gas is `6,085,570`, so the octic model row is not an exact anchor.

#### Precompile-backed octic experiment

A local-only precompile-backed octic verifier variant lives under `src/whir/k22_jb100_lir6_ff4_rsv1_precompile_phase1/`. It is an A/B measurement target for generic KoalaBear ext8 arithmetic precompiles, not the baseline verifier. It depends on a custom Anvil runner from the [Foundry fork](https://github.com/alxkzmn/foundry/tree/codex/ext8-precompile-runner) and is driven by RPC scripts under `script/run_ext8_precompile_*_rpc.py` and `script/run_ext5_precompile_*_rpc.py`. The proof format, transcript ordering, Merkle layout, and external ABI of the baseline verifier are unchanged. See [AGENTS.md](./AGENTS.md) for the command catalog.

### Gas accounting

**Execution gas** is measured via the Foundry gas tests for the deployed contract entrypoints.

**Total tx gas** is measured from real transaction receipts, then split as:

```text
total tx gas = intrinsic gas (21,000) + calldata gas + execution remainder
```

where:

- `calldata gas = 4 * zero_bytes + 16 * nonzero_bytes`
- `execution remainder = receipt.gasUsed - 21,000 - calldata gas`

The quartic comparison calldata footprint comes from `script/WhirBlobNativeTxBenchmark_lir6_ff5_rsv1.s.sol`. The documented octic production calldata footprint comes from `script/WhirBlobNativeTxBenchmark_k22_jb100_lir6_ff4_rsv1.s.sol`. See the [`tx-gas-benchmarking` skill](./.agents/skills/tx-gas-benchmarking/SKILL.md) for the measurement workflow.

For `sol-whir`, the tx numbers come from the [broadcast artifact committed in privacy-ethereum/sol-whir](https://github.com/privacy-ethereum/sol-whir/blob/main/broadcast/Verify.s.sol/31337/run-latest.json). The `sol-whir` benchmark harness does not compile under the toolchain used for this measurement (`stack too deep` / Yul stack-too-deep), so that committed measurement remains the source of truth.

`sol-spartan-whir` Foundry build settings for the quartic comparison contract are `via_ir = true`, `optimizer = true`, and `optimizer_runs = 833`. The recorded `WhirVerifier4` deployed bytecode size is `24,008` bytes.

## License

MIT
